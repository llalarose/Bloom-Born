{
  "name": "bloom-bond-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "BLOOM BOND H5 + AI backend MVP",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "check": "node --check server/index.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
